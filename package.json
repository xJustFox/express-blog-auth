{
  "name": "express-blog-auth",
  "version": "1.0.0",
  "description": "Creiamo le seguenti rotte:\r - `GET /home` \r - `GET /posts` (index)\r - `POST /posts` (store)\r - `GET /posts/:slug` (show)",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.5"
  },
  "dependencies": {
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "slugify": "^1.6.6"
  }
}
